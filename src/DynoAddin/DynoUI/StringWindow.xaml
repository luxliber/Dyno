<local:ValueWindow
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:DynoUI" x:Class="DynoUI.StringWindow"
        mc:Ignorable="d" d:DesignWidth="299"
        >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/MetroDark.MSControls.Core.Implicit.xaml" />
                <ResourceDictionary Source="Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
           
        </ResourceDictionary>
    </Window.Resources>
    <Grid x:Name="ValueWindowContent" >
       
         <!--   <TextBox Name="ValueTextBox" Text="{Binding Path=ValueText, RelativeSource={ RelativeSource Mode=FindAncestor, AncestorType=local:ValueWindow }}" VerticalContentAlignment="Center" HorizontalContentAlignment="Center">
        
        </TextBox> -->
        <ComboBox Name="ValueComboBox" Text="{Binding Path=ValueText, RelativeSource={ RelativeSource Mode=FindAncestor, AncestorType=local:ValueWindow }}" IsEditable="True" ItemsSource="{Binding  Path=Values, RelativeSource={ RelativeSource Mode=FindAncestor, AncestorType=local:ValueWindow }}" HorizontalContentAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Border HorizontalAlignment="Stretch">
                        <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center"/>
                    </Border>
                </DataTemplate>
            </ComboBox.ItemTemplate>
            <ComboBox.Style>
                <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Type ComboBox}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=Values, RelativeSource={ RelativeSource Mode=FindAncestor, AncestorType=local:ValueWindow }}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.Style>
        </ComboBox>
    </Grid>
   
    
</local:ValueWindow>