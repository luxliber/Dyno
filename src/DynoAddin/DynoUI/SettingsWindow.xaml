<Window
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    x:Class="DynoUI.SettingsWindow" 
             mc:Ignorable="d" Height="500" Width="600" Title="Dyno Settings" WindowStartupLocation="CenterScreen" Closing="Window_Closing">
    <Window.Resources>
        <Style x:Key="FolderCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="false"/>
            <Setter Property="FocusVisualStyle"    Value="{DynamicResource CheckBoxFocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Border x:Name="Border" VerticalAlignment="Center" HorizontalAlignment="Center" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                                    <Path Width="12" Height="12" Stretch="Uniform" x:Name="CheckMark" Stroke="Black" StrokeThickness="2" Data="M 0 4 L 2 7 L 7 0" Margin="0,0,0,5"/>
                                </Border>
                            </BulletDecorator.Bullet>
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="false">
                                <Setter TargetName="CheckMark" Property="Stroke" Value="LightGray" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="CheckMark" Property="Stroke" Value="LightGray" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DarkBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource PressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Margin="5" Grid.Row="0">
            <Label Content="Dynamo workspaces and Dyno presets storage folders:" VerticalAlignment="Top"/>
            <Button x:Name="AddButton" FontSize="16" VerticalContentAlignment="Center" VerticalAlignment="Top" HorizontalAlignment="Right" Width="25" Height="25"
			x:FieldModifier="public">
                <Path Data="M0.5,0 L0.5,1 M0,0.5 L1,0.5" Stretch="Fill" Stroke="Black" StrokeThickness="1" Width="10" Height="10" />
            </Button>
        </Grid>
        <ListBox Margin="5" Grid.Row="1" x:Name="StorageFoldersListBox" ItemsSource="{Binding StorageFoldersZip, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="5" VerticalAlignment="Stretch">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,5"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="0,0,0,1" BorderBrush="LightGray">
                        <Grid Height="25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock VerticalAlignment="Center" Name="path"  Margin="0" Padding="10,0,0,0" Text="{Binding Path}"/>
                            <Border Grid.Column="1" BorderThickness="1,0,0,0" BorderBrush="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <CheckBox Style="{StaticResource FolderCheckBoxStyle}" Tag="{Binding Path}" IsChecked="{Binding Status}" BorderThickness="0" Width="29" Height="29"/>
                            </Border>
                            <Border Grid.Column="2" BorderThickness="1,0,0,0" BorderBrush="LightGray">
                                <Button Cursor="Hand" Tag="{Binding  }" Name="browse" Background="Transparent" BorderThickness="0" Click="Browse_OnClick">...</Button>
                            </Border>
                            <Border Grid.Column="3" BorderThickness="1,0,0,0" BorderBrush="LightGray">
                                <Button Cursor="Hand" Click="DeleteButtonOnClick" Tag="{Binding Path}" Name="delete" Background="Transparent" BorderThickness="0">
                                    <Path x:Name="Cross" Data="M0,0 L1,1 M0,1 L1,0" Stretch="Fill" Stroke="Black" StrokeThickness="1" Width="8" Height="8" />
                                </Button>
                            </Border>
                        </Grid>
                    </Border>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Orientation="Vertical" Margin="2,5,0,5" Grid.Row="2" >
          
            <GroupBox Header="General" Margin="0,0,0,5">
                <StackPanel>
                    <CheckBox Margin="2" x:Name="AutoUpdatesCheckBox"
                              Content="Check for program updates automatically"
                              HorizontalAlignment="Left" VerticalAlignment="Top"
                              IsChecked="{Binding IsCheckUpdates, Mode=TwoWay}"
                              x:FieldModifier="public" />
                    
                    <CheckBox Margin="2"
                              Content="Show Dynamo file version in the workspace line" HorizontalAlignment="Left"
                              VerticalAlignment="Top" IsChecked="{Binding IsShowDynamoFileVersion, Mode=TwoWay}" />

                    <CheckBox Margin="2"
                              Content="Show Dyno splash screen when Dynamo core starts" HorizontalAlignment="Left"
                              VerticalAlignment="Top" IsChecked="{Binding IsShowSplashScreen, Mode=TwoWay}" />
                </StackPanel>
            </GroupBox>
            <GroupBox Header="Buttons">
                <StackPanel>
                    <CheckBox Margin="2"
                              Content="Show Dyno button on the 'Add-ins' tab" HorizontalAlignment="Left"
                              VerticalAlignment="Top" IsChecked="{Binding IsShowDynoAddinButton, Mode=TwoWay}" />
                    <CheckBox Margin="2"
                              Content="Show Dyno button on the 'Manage' tab" HorizontalAlignment="Left"
                              VerticalAlignment="Top" IsChecked="{Binding IsShowDynoManageButton, Mode=TwoWay}" />
                    <CheckBox Margin="2"
                              Content="Hide Dynamo Player button on the 'Manage' tab" HorizontalAlignment="Left"
                              VerticalAlignment="Top" IsChecked="{Binding IsHidePlayerButton, Mode=TwoWay}" />
                </StackPanel>
            </GroupBox>


        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Grid.RowSpan="4">
            <Button Margin="5" x:Name="ApplyButton" Content="OK" HorizontalAlignment="Right" Width="75"
			x:FieldModifier="public" Height="23" VerticalAlignment="Bottom" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
